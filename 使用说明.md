# Horizon 使用说明（数字经济博主版）

## 项目位置
`D:\工作专区\00codex专区\Horizon`

## 已为你完成
- 运行环境：uv + Python 3.12 依赖已装好（`uv sync` 已完成）
- 处理档案：新增 `profiles/digital-economy/`（数字经济雷达），匹配、评分、内容区块均按「数字经济 / 数据电商 / 数商创业」定制
- 信息源：`data/config.json` 已配置
  - 中文资讯（已验证可用）：白鲸出海（跨境电商）、创业邦、钛媒体、雷锋网、少数派、IT之家、爱范儿
  - 国际资讯：Hacker News、Simon Willison、OpenAI Blog、Product Hunt
  - GDELT 全球新闻检索（关键词：数字经济/跨境电商/数字贸易/e-commerce）
- 日报平衡：每天最多 20 条，数字经济类优先（8 条），AI 类 4 条，科技类 4 条

## 你需要做的（只需一步）
1. 打开 `D:\工作专区\00codex专区\Horizon\.env`
2. 在某个 API Key 行填入你的密钥（推荐 DeepSeek，见下方）
3. 若你用的不是 DeepSeek，还需要同步改 `data/config.json` 里的 `ai` 段：
   - `provider` 与 `api_key_env` 需对应（如 OpenAI 用 `"provider": "openai"` + `OPENAI_API_KEY`）

## 密钥去哪申请（任选其一）
| 提供商 | 申请地址 | 说明 |
|--------|----------|------|
| DeepSeek（推荐） | https://platform.deepseek.com | 便宜、国内直连 |
| 通义千问 | https://dashscope.aliyun.com | 阿里云 |
| 豆包 | https://console.volcengine.com/ark | 火山引擎 |
| OpenAI | https://platform.openai.com | 需境外网络 |
| Claude | https://console.anthropic.com | 需境外网络 |

## 运行
```bash
cd D:\工作专区\00codex专区\Horizon
.venv\Scripts\horizon.exe --hours 24 -l INFO
```
日报生成在 `data\summaries\`，同时会复制到 `docs\`（用于 GitHub Pages 站点）。

## 可选增强
- 飞书/钉钉通知：在群机器人里加自定义关键词 `Horizon`，把 Webhook 地址填入 `.env` 的 `HORIZON_WEBHOOK_URL`，再把 `data/config.json` 的 `webhook.enabled` 改为 `true`（platform 可设 `feishu`）
- Telegram / Reddit / Twitter：当前网络无法直连，保持关闭；如使用代理可自行打开 `data/config.json` 对应 `enabled`
- 修改信息源：编辑 `data/config.json` 的 `sources.rss` 数组即可（name/url/category/profile）

## 自动日报（可选）
参考项目自带 `.github/workflows/daily-summary.yml`，把项目推到 GitHub 后可定时生成并部署到 GitHub Pages。

## 输出语言
日报语言在 `data/config.json` 的 `ai.languages` 设置（当前为 `["zh"]` 中文；想中英双语改为 `["zh","en"]`）。

## 运行记录（2026-08-13 已完成首次验证）
- 中文日报：`data\summaries\horizon-2026-08-12-zh.md`（126 条信息源 → 精选 19 条）
- 英文日报：`data\summaries\horizon-2026-08-12-en.md`
- 已同时发布到 `docs\_posts\`（GitHub Pages 用）
- DeepSeek 密钥已填入 `.env` 并验证可用